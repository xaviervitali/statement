<form method="post" action="{{ path('app_appreciation_delete', {'id': appreciation.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ appreciation.id) }}">
    <button class="btn">Delete</button>
</form>
